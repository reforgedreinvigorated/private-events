<div class = "container">
    <nav>
        <ul><%= button_to "Home", root_path, method: :get%></ul>
        <ul><%= button_to "Sign Up", new_user_registration_path, method: :get%></ul>
        <% if user_signed_in? %>
            <ul><%= button_to "Sign Out", destroy_user_session_path, method: :delete%></ul>
            <ul><%= button_to "Create Event", new_event_path, method: :get%></ul>
        <% else %>
            <ul><%= button_to "Sign In", new_user_session_path, method: :get%></ul>
        <% end %>
    </nav>

</div>